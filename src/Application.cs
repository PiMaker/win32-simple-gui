namespace Win32.SimpleGui;

public class Application
{
    // create a new window and open it
    public Window CreateWindow(string title, int width, int height)
    {
    }

    // return true if the user clicked "OK", false if "Cancel"
    public bool MessageBox(string title, string message)
    {
    }
}
